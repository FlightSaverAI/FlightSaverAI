import { Route } from '@angular/router';
import { HomeContainerComponent } from '@flight-saver/home/features';

export const homeRoutes: Route[] = [
  {
    path: '',
    component: HomeContainerComponent,
  },
];
