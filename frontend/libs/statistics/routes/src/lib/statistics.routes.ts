import { Route } from '@angular/router';
import { StatisticsComponent } from '@flight-saver/statistics/features';

export const statisticsRoutes: Route[] = [
  {
    path: '',
    component: StatisticsComponent,
  },
];
